<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 마이크 권한 (실시간 음성 수집 필수) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 내부 저장소 녹음 파일 저장 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- 백그라운드 처리 중 화면 켜짐 유지 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 포그라운드 서비스 (백그라운드 녹음) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- Android 13+ 알림 런타임 권한 (포그라운드 서비스 알림 필수) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.microphone" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="SpeechCoach"
        android:roundIcon="@drawable/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpeechCoach">

        <!-- 메인 발표 화면 (HUD) -->
        <activity
            android:name=".ui.hud.PresentationActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 발표 종료 후 리포트 화면 -->
        <activity
            android:name=".ui.report.ReportActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- 사용자 기준 목소리 캘리브레이션 화면 -->
        <activity
            android:name=".ui.hud.CalibrationActivity"
            android:exported="false" />

        <!-- 백그라운드 녹음 포그라운드 서비스 -->
        <service
            android:name=".audio.RecordingService"
            android:foregroundServiceType="microphone"
            android:exported="false" />

    </application>
</manifest>
